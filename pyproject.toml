[project]
name = "noteri-runtime"
version = "0.1.0"
description = "Runtime específico do host Noteri"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
