# CI bootstrap

O workflow de CI foi estabelecido primeiro na `main` para que Pull Requests subsequentes sejam validadas pelo GitHub Actions desde o primeiro incremento.

Este commit existe para disparar a validação da PR inicial após o bootstrap do workflow.
